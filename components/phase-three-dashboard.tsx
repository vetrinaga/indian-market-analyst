'use client'

import { useState } from 'react'
import { Archive, BookOpen, CheckCircle2, CircleHelp, FileClock, Plus, RotateCcw, Save, Target } from 'lucide-react'

type Entry = { id: number; asset: string; thesis: string; evidence: string; confidence: string; date: string; status: 'Open' | 'Review' }
const initialEntries: Entry[] = [{ id: 1, asset: 'Imported equity portfolio', thesis: 'Keep quality growth exposure while reducing hidden financials overlap.', evidence: 'Imported holding statement + portfolio X-ray', confidence: 'Medium', date: '08 Sep 2026', status: 'Open' }, { id: 2, asset: 'Government of India 10Y', thesis: 'Use as a stabilizer while equity risk remains above the target range.', evidence: 'Sovereign issuer + maturity profile', confidence: 'High', date: '02 Sep 2026', status: 'Review' }]
export function PhaseThreeDashboard() { const [entries, setEntries] = useState(initialEntries); const [asset, setAsset] = useState(''); const [thesis, setThesis] = useState(''); const [evidence, setEvidence] = useState(''); const [confidence, setConfidence] = useState('Medium'); const [selected, setSelected] = useState<Entry | null>(initialEntries[0]); const openCount = entries.filter(entry => entry.status === 'Open').length; const addEntry = () => { if (!asset.trim() || !thesis.trim()) return; const next = { id: Date.now(), asset: asset.trim(), thesis: thesis.trim(), evidence: evidence.trim() || 'User-entered thesis; evidence not attached', confidence, date: 'Today', status: 'Open' as const }; setEntries(current => [next, ...current]); setSelected(next); setAsset(''); setThesis(''); setEvidence('') }; return <section className="mt-7 overflow-hidden rounded-2xl border border-border bg-card shadow-sm"><div className="border-b border-border bg-[#172033] px-5 py-5 text-white md:px-7"><div className="flex items-start justify-between gap-4"><div><div className="flex items-center gap-2 font-mono text-[10px] uppercase tracking-[.18em] text-sky-300"><FileClock size={14}/> Phase 3 intelligence</div><h2 className="mt-2 text-2xl font-semibold tracking-tight">Investment journal & decision replay</h2><p className="mt-1 max-w-3xl text-xs leading-5 text-white/65">Turn each investment decision into a testable record. Revisit the evidence, confidence, and outcome instead of relying on memory.</p></div><Archive className="text-sky-300" size={22}/></div></div><div className="grid gap-5 p-5 md:p-7 xl:grid-cols-[.8fr_1.2fr]"><div className="rounded-xl border border-border p-5"><div className="flex items-center gap-2 text-sm font-semibold text-primary"><Plus size={16}/> Record a decision</div><p className="mt-1 text-xs text-muted-foreground">Write what you believed before the market gave you an answer.</p><div className="mt-4 space-y-3"><input value={asset} onChange={event => setAsset(event.target.value)} placeholder="Asset or portfolio name" className="w-full rounded-md border border-input bg-background px-3 py-2 text-xs outline-none focus:ring-2 focus:ring-teal-500/30"/><textarea value={thesis} onChange={event => setThesis(event.target.value)} placeholder="Why are you holding, buying, or avoiding it?" rows={3} className="w-full resize-none rounded-md border border-input bg-background px-3 py-2 text-xs outline-none focus:ring-2 focus:ring-teal-500/30"/><textarea value={evidence} onChange={event => setEvidence(event.target.value)} placeholder="Evidence, source, or numbers supporting the view" rows={2} className="w-full resize-none rounded-md border border-input bg-background px-3 py-2 text-xs outline-none focus:ring-2 focus:ring-teal-500/30"/><label className="block text-[11px] font-medium text-primary">Confidence<select value={confidence} onChange={event => setConfidence(event.target.value)} className="mt-1 w-full rounded-md border border-input bg-background px-3 py-2 text-xs"><option>Low</option><option>Medium</option><option>High</option></select></label><button onClick={addEntry} className="inline-flex w-full items-center justify-center gap-2 rounded-md bg-primary px-3 py-2 text-xs font-semibold text-primary-foreground"><Save size={14}/> Save decision</button></div><div className="mt-4 rounded-lg border border-sky-200 bg-sky-50 p-3 text-[10px] leading-4 text-sky-950">Good journal entries are falsifiable: include what should happen, by when, and what would change your mind.</div></div><div className="space-y-5"><div className="grid gap-3 sm:grid-cols-3"><Mini label="Open decisions" value={String(openCount)} note="Need follow-up"/><Mini label="Evidence attached" value="83%" note="Across entries"/><Mini label="Replay readiness" value="Medium" note="Add dated outcomes"/></div><div className="rounded-xl border border-border p-5"><div className="flex items-center justify-between"><div><div className="flex items-center gap-2 text-sm font-semibold text-primary"><BookOpen size={16}/> Decision log</div><p className="mt-1 text-xs text-muted-foreground">Select an entry to compare the original view with today.</p></div><span className="rounded-full bg-muted px-2 py-1 font-mono text-[10px]">{entries.length} entries</span></div><div className="mt-4 space-y-2">{entries.map(entry => <button key={entry.id} onClick={() => setSelected(entry)} className={`w-full rounded-lg border p-3 text-left transition ${selected?.id === entry.id ? 'border-teal-400 bg-teal-50/60' : 'border-border hover:bg-muted/50'}`}><div className="flex items-start justify-between gap-3"><div><div className="text-xs font-semibold text-primary">{entry.asset}</div><div className="mt-1 line-clamp-1 text-[11px] text-muted-foreground">{entry.thesis}</div></div><span className={`shrink-0 rounded px-2 py-1 text-[9px] font-semibold ${entry.status === 'Open' ? 'bg-amber-100 text-amber-900' : 'bg-teal-100 text-teal-900'}`}>{entry.status}</span></div><div className="mt-2 flex gap-3 font-mono text-[9px] text-muted-foreground"><span>{entry.date}</span><span>Confidence: {entry.confidence}</span></div></button>)}</div></div>{selected && <Replay entry={selected}/>}</div></div></section> }
function Replay({ entry }: { entry: Entry }) { return <div className="rounded-xl border border-border bg-muted/20 p-5"><div className="flex items-center gap-2 text-sm font-semibold text-primary"><RotateCcw size={16}/> Replay: {entry.asset}</div><div className="mt-4 grid gap-3 md:grid-cols-2"><div className="rounded-lg border border-border bg-card p-3"><div className="text-[10px] uppercase tracking-wider text-muted-foreground">Original thesis</div><p className="mt-2 text-xs leading-5 text-primary">{entry.thesis}</p></div><div className="rounded-lg border border-border bg-card p-3"><div className="text-[10px] uppercase tracking-wider text-muted-foreground">Evidence recorded</div><p className="mt-2 text-xs leading-5 text-muted-foreground">{entry.evidence}</p></div></div><div className="mt-4 grid gap-2 text-xs sm:grid-cols-3"><div className="flex items-center gap-2 text-teal-700"><CheckCircle2 size={14}/> What changed?</div><div className="flex items-center gap-2 text-amber-800"><CircleHelp size={14}/> Was the risk priced?</div><div className="flex items-center gap-2 text-primary"><Target size={14}/> Is the thesis intact?</div></div><div className="mt-4 rounded-lg border border-amber-300/70 bg-amber-50 p-3 text-[10px] leading-4 text-amber-950">Outcome tracking is intentionally manual until dated broker prices and corporate actions are connected. Do not treat an open thesis as a recommendation.</div></div> }
function Mini({ label, value, note }: { label: string; value: string; note: string }) { return <div className="rounded-xl border border-border p-4"><div className="text-[10px] uppercase tracking-wider text-muted-foreground">{label}</div><div className="mt-2 font-mono text-lg font-bold text-primary">{value}</div><div className="mt-1 text-[10px] text-muted-foreground">{note}</div></div> }
